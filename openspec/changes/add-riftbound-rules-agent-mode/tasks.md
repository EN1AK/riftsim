# Tasks: Add Riftbound Rules Agent Mode

## 1. Agent loop core

- [x] 1.1 Create `scripts/rag/agent_loop.py` with the planner protocol and loop driver: injectable planner callable (default: OpenAI-compatible chat call using existing `RAG_LLM_MODEL`/`OPENAI_API_KEY`/`OPENAI_BASE_URL` envs), one JSON `{tool, arguments}` action per step, observation feedback, and a per-question run state. Verify with `$env:PYTHONUTF8='1'; python -m pytest scripts/rag/tests/test_agent_loop.py -q` using a scripted fixture planner (no network) covering: valid multi-step sequence executes tools in order and observations feed back.
- [x] 1.2 Implement tool dispatch over a fresh `EvidencePool` per question, reusing `rag_query` functions: `resolve_cards`, `get_card_rules`, `search_rules` (with ≤2 expansion texts from resolved cards), `lookup_rule` — every call adds returned rows to the pool and appends a trace entry. Verify via pytest: pool accumulates distinct rows across steps (3 + 5 with 1 overlap → 7) and trace records `step/tool/arguments/ok` entries.
- [x] 1.3 Implement action bounds: `RAG_AGENT_MAX_STEPS` (default 4, clamped 1–8 — e.g. 99 → 8), duplicate `(tool, arguments)` signature rejection with an explanatory observation, and two-consecutive-invalid-output stop. Verify via pytest cases for each: step limit stops the loop, clamping, duplicate action not executed, two unparseable outputs stop the loop.

## 2. Submission validation and fallback

- [x] 2.1 Implement `submit_answer` as the only terminal action: regex-scan the answer for `[…]` markers plus the `citations` list, require every entry to be a `rule_id` in the pool, and on failure return an error observation naming the bad citation while steps remain. Verify via pytest: valid submit ends the loop with `exhausted=False`; unknown citation rejected with a naming observation and the planner may resubmit (fixture planner second submit succeeds).
- [x] 2.2 Implement the deterministic fallback: on loop end without a validated submit, call `rag_query.generate(query, pool.rows(), model_name, timeout=...)` over the pool; empty pool → return the existing `EMPTY_RETRIEVAL_ANSWER`-equivalent fixed answer with the `检索结果为空` warning; result marked `exhausted=True`; planner LLM call failure also lands here. Verify via pytest: exhausted loop generates over gathered pool rows; empty-pool case returns the fixed answer plus warning without calling the generator; a raising fixture planner still yields a structured result instead of an exception.

## 3. Service contract v2 wiring

- [x] 3.1 Extend `parse_request` in `scripts/rag/rag_server.py` to accept `mode` (`agent` default | `oneshot`; other values → `ValueError`) and `trace` (bool, default false), keep legacy `query`/`top_k` validation and unknown-key tolerance. Verify via pytest server cases: legacy body accepted with defaults; `"turbo"` mode → 400; extra keys ignored.
- [x] 3.2 Wire the agent path into `RagService`: lazily build `CardResolver` (from `RAG_CARDS_DB_PATH`, degrading to id-only matching with an stderr warning when the cards DB is missing), pre-run deterministic resolution on the query for the planner prompt, run the agent loop in `agent` mode, and keep the existing one-shot path verbatim for `oneshot`. Verify via pytest with a stubbed loop service: `agent` mode invoked by default and for explicit `mode=agent`; `oneshot` calls only the one-shot path; `--preload` also warms the cards index.
- [x] 3.3 Assemble the v2 response: agent responses include `mode`, `exhausted`, `resolved_cards`, `coverage {unresolved_mentions, ambiguous_mentions}`, and `trace` only when requested; `oneshot` responses keep the exact legacy shape; error semantics (`400`/`404`/`405`/`500`, `/healthz`) unchanged. Verify via pytest HTTP-scope cases: agent response shape, `trace` present only on request, coverage fields populated from a fixture resolver (resolved/ambiguous/unresolved mention each surfaced), oneshot omits v2 keys, generation failure still → 500.

## 4. Verification

- [x] 4.1 Run the full suite `$env:PYTHONUTF8='1'; python -m pytest scripts/rag/tests -q`, `python -m py_compile scripts/rag/agent_loop.py scripts/rag/rag_server.py scripts/rag/rag_query.py`, and `openspec validate add-riftbound-rules-agent-mode --strict`; confirm all pass.
- [x] 4.2 Live smoke (DeepSeek `deepseek-chat` via `OPENAI_BASE_URL=https://api.deepseek.com`): server `--preload` 启动后四场景验证（请求/响应存档于 `workspace/rag/smoke_v2_*`）：① 卡片问题 agent+trace——海兽钓钩精确解析为 OGN-242、覆盖度字段、3 步工具 trace 完整、引用合法；② 纯规则问题——不带 mode 默认 agent，诚实引用 5 条 rule_id；③ 垃圾输入——planner 选择零引用合法提交拒绝作答（保持的设计语义：提交不为空引用恒真校验），`exhausted: false`；空池固定答案路径由单测 `test_empty_pool_fixed_answer_without_generation` 覆盖；④ oneshot——严格 legacy 三字段形状。`GET /healthz` 返回 `200 {"ok": true}`。调优观察：deepseek-chat 在 max_steps=4 内未走到带引用 submit（均落 fallback，回答质量仍好），后续可按 trace 数据调整步数或 planner 提示。
