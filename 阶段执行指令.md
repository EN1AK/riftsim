# 各阶段可单独发给 Coding Agent 的指令

所有阶段首先读取根目录 `AGENTS.md`，严格遵守来源优先级、规则 ID 和隐藏信息约束。每个阶段新开会话时先读取前阶段实际产物与报告，禁止虚构已完成内容。

## 第一次：阶段 0–2，规则、训练架构及 UI 协议

请执行 AGENTS.md 阶段 0–2，不写大规模业务实现。重点在现有 `workspace/final/rules.md`、`rules.db`、`cards_bilingual.db` 上建立规则规格和测试矩阵，同时输出 `docs/training_architecture.md`、`docs/visualization_spec.md`、`docs/api_contract.md`。明确 headless 引擎、向量化自博弈、训练适配器、trace schema、隐藏信息视角、只读 Viewer 的模块边界；给出 UI 草图和第一场可回放自动对局的实现顺序。完成后提供精确文件路径、测试、未解规则及阶段 3 的最小计划，停止。

## 第二次：阶段 3，最小 headless 引擎 + 自动对局 + trace

读取已核实规格及阶段 2 API，先测试后实现最小规则引擎。实现 reset/observe/legal_actions/step/snapshot/restore、确定性 RNG、规则关联事件、随机/脚本策略自动对局 runner、可重放 fixture trace。验证公开观察不泄漏隐藏信息、已选动作合法、同 seed 与动作重放一致。先让一场基础规则对局自动完成，再增加卡牌覆盖。提交实际 SPS 基线（注明硬件/日志等级），不要实现 PPO 或复杂 UI。

## 第三次：阶段 4，只读对局 Viewer + 卡牌效果

先读取阶段 3 的真实 trace 构建最小 Viewer：按规则适配场面布局，公开场面、玩家资源/区域、时间轴、当前决策、动作前后变化、事件/规则来源及最终结果；支持逐步/播放/筛选 match_id。策略分布/value 未记录时标注无数据。确保 Viewer 不 import 引擎、不支持直接人类出牌、不泄漏隐藏信息。随后按规则依赖扩展卡牌效果和覆盖测试。提供 Viewer 启动命令、实际 fixture、截图或渲染检查和覆盖报告。

## 第四次：阶段 5，完整自动对局与独立审计

使用规则知识库独立构造测试，验证核心、卡牌交互、完整自动对局、随机性、状态不变量、snapshot/replay 一致性。批量跑随机/脚本对手，对异常场次自动导出 match_id、seed 和最小 trace，检查 Viewer 每步公开状态与引擎一致。输出规则覆盖/卡牌覆盖/回放验证/测试报告。

## 第五次：阶段 6，自博弈训练器与分析面板

使用已有 PPO 框架（若仓库有）或最小兼容 trainer，接入 headless env 的观察、legal action mask、奖励、终局、batch/vector env、checkpoint 与 eval。训练默认不开 Viewer，按可配置采样率保存 trace；将策略 top-k / 选中概率 / value（确实有时）、奖励、规则事件与 policy/checkpoint 关联。做 SPS、回报、胜率（注明对手/先后手）、对局长度、非法动作、熵/KL/value loss（如有）的监控；可从异常指标跳到回放局。基准测试 trace off/summary/sampled/debug 的训练开销；支持日后 clone/snapshot/restore 驱动可信搜索而不泄漏全知状态。提交 train/eval/viewer 命令和至少一次实际端到端烟雾测试。
